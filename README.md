# mcp-new

Composable MCP server that can host multiple services. The default configuration
exposes both an IBAN validation tool and a simple echo tool for connectivity
checks through the shared `app_mcp.py` entrypoint.
