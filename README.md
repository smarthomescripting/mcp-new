# mcp-new

Composable MCP server that can host multiple services. The default configuration
exposes an IBAN validation tool, a website fetch tool that returns plain text,
and a simple echo tool for connectivity checks through the shared `app_mcp.py`
entrypoint.
